 
    <!-- Preloader -->
    <div id="preloader">
        <div class="medilife-load"></div>
    </div>

    <!-- ***** Header Area Start ***** -->
    <header class="header-area">
        <!-- Top Header Area -->
        <div class="top-header-area">
            <div class="container h-100">
                <div class="row h-100">
                    <div class="col-12 h-100">
                        <div class="h-100 d-md-flex justify-content-between align-items-center">
                            <p>Welcome to <span>PhysioPlus</span> Center</p>
                            <p>Opening Hours : Monday to Saturday - 9am to 8pm Contact : <span>+91 94280 54260</span></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Main Header Area -->
        <div class="main-header-area" id="stickyHeader">
            <div class="container h-100">
                <div class="row h-100 align-items-center">
                    <div class="col-12 h-100">
                        <div class="main-menu h-100">
                            <nav class="navbar h-100 navbar-expand-lg">
                                <!-- Logo Area  -->
                                <h2 style="text-shadow: 1px 1px 4px black;"><font color="white">Physio<font color="#0067f4">Plus</font></font></h2>

                                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#medilifeMenu" aria-controls="medilifeMenu" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>

                                <div class="collapse navbar-collapse" id="medilifeMenu">
                                    <!-- Menu Area -->
                                    <ul class="navbar-nav ml-auto">
                                        <li class="nav-item <% if(page == 'home'){ %> active <% } %>">
                                            <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
                                        </li>
                                        <li class="nav-item <% if(page == 'about'){ %> active <% } %>">
                                            <a class="nav-link" href="/about-us">about</a>
                                        </li>
                                        <li class="nav-item <% if(page == 'services'){ %> active <% } %>">
                                            <a class="nav-link" href="/services">Services</a>
                                        </li>
                                        <li class="nav-item <% if(page == 'contact'){ %> active <% } %>">
                                            <a class="nav-link" href="/contact">Contact</a>
                                        </li>
                                        <li class="nav-item <% if(page == 'login'){ %> active <% } %>">
                                            <a class="nav-link" href="/login">Login</a>
                                        </li>
										<li class="nav-item">
                                            <a class="nav-link" href="/logout">Logout</a>
                                        </li>
                                    </ul>
                                    <!-- Appointment Button -->
                                    <a href="/#appoinment" class="btn medilife-appoint-btn ml-15">For <span>Appoinment Booking</span></a>
									<a href="#" class="btn medilife-appoint-btn ml-15">Welcome, <span>User</span></a>
                                </div>
                            </nav>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </header>
    <!-- ***** Header Area End ***** -->